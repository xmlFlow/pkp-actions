on: [push]

jobs:
  validate:
    runs-on: ubuntu-latest
    name: pkp-integration
    steps:
      - uses: actions/checkout@v4
      
      - uses: xmlFlow/pkp-actions@v133
        with:
          pkp-application: 'ojs'
          pkp-repository: 'withanage'
          pkp-branch: 'issue#9214'